

public class next {
    /*
    smallestnode = head;
    beforesmallest = head;
    beforeCandidate = nullcurrent = head;

    remove

    while(curr.nexr 1= null){
        bC = curr
        curr = curr.next
        if (curr.value < smallestNode.vallue){
            smallestNode = current;
            bSmallest = beforeCandidate;
        }

    }
    */
}
